#postgres configuration
spring.datasource.url=jdbc:postgresql://dpg-d2k6mvje5dus738ssjng-a.singapore-postgres.render.com/wealthtrack
spring.datasource.username=${wealthTrack_postgres_name}
spring.datasource.password=${wealthTrack_postgres_password}
spring.datasource.driver-class-name=org.postgresql.Driver



# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server configuration
server.servlet.context-path=/api/v1.0

# Email Configuration
spring.mail.host=smtp-relay.sendinblue.com
spring.mail.port=587
spring.mail.username=${mail_userName}
spring.mail.password=${mail_password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.from=${email_name}
spring.mail.properties.mail.debug=true

# Frontend / Backend URLs
wealth.track.fronted.url=${wealthTrack_fronted_url}
app.activation.url=${wealthTrack_backend_url}

